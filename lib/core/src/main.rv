import stdio;
import iter;

pub fn test() -> u64 {
    let first = new NumberIter {
        current: 0,
        end: 1,
    };
    let second = new NumberIter {
        current: 2,
        end: 3,
    };
    let value = [first, second];
    return value[0].end;
}