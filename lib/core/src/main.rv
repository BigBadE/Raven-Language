import iter;
import iter::Iter;
import stdio;

#[extern]
pub fn main() -> i64 {
    let iter = new NumberIter {
        current: 1,
        end: 5,
    };

    let i = 0;
    for value in iter {
        printf(value);
    }
    return 0;
}