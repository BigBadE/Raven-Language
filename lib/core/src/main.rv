import iter;
import stdio;
import math::Cast;

#[extern]
pub fn main() -> u64 {
    let iter = new NumberIter {
        current: 1,
        end: 6,
    };

    printf("This is a printed string! Wowie!");
    let i = 0;
    for value in iter {
        i = i + value;
    }
    return i.cast();
}