import stdio;
import iter;

#[extern]
pub fn main() -> u64 {
    let first = new NumberIter {
        current: 0,
        end: 1,
    };
    let second = new NumberIter {
        current: 2,
        end: 3,
    };
    let value = [first, second];
    return value[1].end;
}