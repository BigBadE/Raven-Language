import iter;
import iter::Iter;
import stdio;

#[extern]
pub fn main() -> i64 {
    let iter = new NumberIter {
        current: 41,
        end: 47,
    };

    let i = 0;
    for value in iter {
    }
    return 0;
}