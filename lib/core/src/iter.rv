pub trait Iter<T> {
    pub fn next<T>(self: Iter<T>) -> T;

    pub fn is_end<T>(self: Iter<T>) -> bool;
}

pub struct NumberIter<T> {
    pub last: T;
    pub end: T;

    pub fn new<T>(start: T, end: T) -> NumberIter<T> {
        return new NumberIter<T> {
            last: start,
            end: end
        };
    }
}
