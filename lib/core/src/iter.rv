pub struct NumberIter<T> {
    pub last: T;
    pub end: T;

    pub fn new<T>(start: T, end: T) -> iter::NumberIter<T> {
        return iter::NumberIter {
            last: start,
            end: end
        };
    }

    pub fn next<T>(self: iter::NumberIter<T>) -> T {
        return self.last;
    }
}