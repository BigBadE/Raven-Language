fn test() -> bool {
    let temp = 5;
    let value = (1 + 2) - 3 / 4 * 5;
    temp += 2;
    temp -= 2;
    temp *= 3;
    temp /= 2;
    temp %= 4;
    // Checks to make sure priority works.
    if 5 / 2 - 1 != 1 {
        return false;
    }
    return !(value != temp);
}