import stdio;
import numbers::Match;

fn main() {
    if 4 == 4 {
        printf("Passed!");
    } else {
        printf("Failed!");
    }

    for thing in my_struct.iter().some_other_call() {

    }
}