name: Rust

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: true
    - name: Add Nightly
      run: rustup toolchain install nightly
    - name: Build
      run: cargo +nightly build --verbose --release --bin magpie
    - name: Build Wrapper
      run: cargo +nightly build --verbose --release --bin magpie-wrapper
    - name: Upload Raven
      uses: actions/upload-artifact@v3.1.3
      with:
        # Artifact name
        name: Raven-linux # optional, default is artifact
        # A file, directory or wildcard pattern that describes what to upload
        path: target/debug/magpie
        # The desired behavior if no files are found using the provided path.
        if-no-files-found: error # optional, default is warn
        # Duration after which artifact will expire in days. 0 means using default retention.
        # Minimum 1 day. Maximum 90 days unless changed from the repository settings page.
        retention-days: 1 # optional
    - name: Upload Magpie wrapper
      uses: actions/upload-artifact@v3.1.3
      with:
        # Artifact name
        name: Magpie-linux-wrapper # optional, default is artifact
        # A file, directory or wildcard pattern that describes what to upload
        path: target/debug/magpie-wrapper
        # The desired behavior if no files are found using the provided path.
        if-no-files-found: error # optional, default is warn
        # Duration after which artifact will expire in days. 0 means using default retention.
        # Minimum 1 day. Maximum 90 days unless changed from the repository settings page.
        retention-days: 1 # optional